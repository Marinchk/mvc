<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Create Collaboration</title>
  <link rel="stylesheet" href="/styles/create-collaboration.css" />
</head>
<body>
  <div class="form-container">
    <h1>New Collaboration</h1>

    <% if (typeof error !== 'undefined' && error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/api/collaborations" method="POST">
      <input type="text" name="title" placeholder="Title" required />
      <textarea name="description" placeholder="Description" rows="4"></textarea>
      <button type="submit">Create</button>
    </form>

    <a class="back" href="/api/collaborations">← Back to list</a>
  </div>
</body>
</html>
