<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Мои коллаборации</title>
  <link rel="stylesheet" href="/styles/dashboard.css">
</head>
<body>
  <div class="container">
    <h1>Добро пожаловать, <%= user.name %>!</h1>
    <h2>Коллаборации, которыми вы владеете:</h2>
    <div class="dashboard-grid">
      <% ownedCollaborations.forEach(collab => { %>
        <div class="collab-card" onclick="window.location.href='/api/collaborations/<%= collab._id %>'">
          <div class="collab-title"><%= collab.title %></div>
          <button class="delete-btn" onclick="event.stopPropagation(); deleteCollaboration('<%= collab._id %>')">×</button>
        </div>
      <% }) %>

      <!-- Кнопка Добавить коллаборацию только среди владельческих -->
      <div class="add-card" onclick="window.location.href='/api/collaborations/new'">+</div>
    </div>

    <h2>Коллаборации, где вы участник:</h2>
    <div class="dashboard-grid">
      <% memberCollaborations.forEach(collab => { %>
        <div class="collab-card" onclick="window.location.href='/api/collaborations/<%= collab._id %>'">
          <div class="collab-title"><%= collab.title %></div>
          <!-- Участники не могут удалять, поэтому кнопки нет -->
        </div>
      <% }) %>
    </div>
  </div>

  <script>
    function deleteCollaboration(id) {
      if (confirm('Вы уверены, что хотите удалить эту коллаборацию?')) {
        fetch(`/api/collaborations/${id}`, {
          method: 'DELETE',
          headers: { 'Content-Type': 'application/json' }
        }).then(res => {
          if (res.ok) {
            location.reload();
          } else {
            alert('Ошибка при удалении.');
          }
        });
      }
    }
  </script>
</body>
</html>
